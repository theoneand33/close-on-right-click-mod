{
  "schemaVersion": 1,
  "id": "right-click-close",
  "version": "${version}",
  "name": "Right Click Close",
  "description": "Closes the current menu when you right-click.",
  "icon": "icon.png",
  "environment": "client",
  "mixins": [
    "right-click-close.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.5",
    "minecraft": "${minecraft_dependency}",
    "java": ">=${java_compatibility}"
  }
}
